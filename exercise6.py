# Variable called types of people which the numbe r10 is assigned
types_of_people = 10
# Assigning the variable x using the f-string
x = f"There are {types_of_people} types of people."
# variable assigned to the string name binary
binary = "binary"
# Another variable name assigned to the string don't having a single quote insde double quotes
do_not = "don't"
# Assigning the variable y using the f-string
y = f"Those who know {binary} and those who {do_not}."
# printing x
print(x)
# printing y
print(y)
# printing using the f-string and passing the variable x to the curly brackets
print(f"I said: {x}")
print(f"I also said: '{y}'")
# Assigning the variable hilarious to false
hilarious = False
# Assigning the variable to a string
joke_evaluation = "Isn't that joke so funny?! {}"
# Print using the format
print(joke_evaluation.format(hilarious))
# Assigning the variable w to a string
w = "This is the left side of..."
e = "a string with a right side."
# concantenating the stings e and w
print(w + e)
